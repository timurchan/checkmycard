sudo docker ps - проверить статус по работающим контейнерам


sudo docker container ps -a		какие есть контейнеры


sudo docker image ls			какие есть имеджы




sudo docker run -d -p 80:8091 --name checkmycard a88236/checkmycard
 - попытка запустить новый контейнер

В общем, не нужно удалять контейнер и потом запускать новый. Можно просто запустить существующий:
sudo docker start checkmycard

не зпустится если был контнейнер с тем же именем
удалить прежний: sudo docker container rm XXXXXXX


sudo docker exec -it checkmycard bash

exit

sudo docker logs -f checkmycard







http://ec2-35-171-88-255.compute-1.amazonaws.com/	
Завёл тебе пользователя 
https://767397770324.signin.aws.amazon.com/console
Timurchan
kqL61^Z!




1. adjust image in 2 places
+2. не сбрасывать картинку при возвращ нзад
+3. создавать каталог на каждую сессию
4. зачищать протухшие сессии
5. вывести версию софта на главной странице
6. сделать подгрузку через 1 кнопку
7. сделать русские надписи
8. nice to have: подтянуть реальный данные про цены и прочее










CSS styles examples/templates
https://freefrontend.com/css-buttons/
https://codepen.io/mark_sottek/pen/ExEKegv









элементы
fileInput
previewContainer
preview
должны быть на одном уровне . наверное в одном контейнере
когда ты показыаешь каждую кнопку - расстояния между кнопками должны быть одинаковы
сначала кнопка выбора файла. потом кнопка Посмотреть. потом кнопка Загрузить

сделай улучшения
1. убери alert window , вместо него когда файл загружен обнови статус - текст под изображениями
2. сделай цвета кнопок в голубых тонах. очень спокойные
3. Choose File - именно эта надпись на сером фоне выгляди ткак стандратная кнопка . улучши это 
4. ВАЖНО: если/когда файл выбранный пользователем содержит 3 на 4 и сразу загружается на север - скрой cropper элемент и preview должно содержать изображение этого посланного файла. т.к. файл сохранился - то ты можешь использовать url загруженного изображения в img для preview
т.е. preview должен сожрежать либо изо после crop операции либо изо загруженного файла. каждый раз обновляться в зависимости о тдействитй пользователя
5. фон всей страницы сделай бежевым
фон основной части с элементами вместо белого - персикового цвета
